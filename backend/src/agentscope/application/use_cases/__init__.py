"""Cas d'utilisation — un objet par intention, avec une seule méthode publique.

Convention : le nom est un verbe à l'impératif (`GetSystemStatus`, `ImportTraceFile`), les
dépendances arrivent par le constructeur, et la méthode d'exécution s'appelle `execute`.
"""

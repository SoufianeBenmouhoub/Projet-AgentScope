# Copier en « .env.local » si besoin. Aucun secret ne doit figurer ici : tout ce qui est
# préfixé par VITE_ est livré au navigateur.

# Base de l'API. À laisser vide en développement : Vite redirige /api vers le back
# (voir vite.config.ts).
VITE_API_BASE_URL=
